syntax = "proto3";

package textservice;

option go_package = "./textpb";

// The service definition
service AiDatService {
  // Accepts a text string and returns the processed text
  rpc ProcessText (AiRequest) returns (AiResponse);
}

// Request payload sent from Go -> Python
message AiRequest {
  string data = 1;
}

// Response payload sent back from Python -> Go
message AiResponse {
  string data = 1;
}
